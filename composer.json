{
  "autoload": {
    "classmap": [
      "src/"
    ]
  },
  "require-dev": {
    "phpunit/phpunit": "^12"
  },
  "scripts": {
      "test": "./vendor/bin/phpunit --bootstrap vendor/autoload.php --testdox tests"
  }
}
